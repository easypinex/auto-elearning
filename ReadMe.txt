1.Check you enviromment install below list:
        1-1.Python 3.7.
        1-2.Python Package-PIL  (After install Python 3.7 , command "pip3 install Pillow" on cmd.exe)
        1-3.Python Package-requests (After install Python 3.7 , command "pip3 install requests" on cmd.exe).
        1-4.Python Package-selenium (After install Python 3.7 , command "pip3 install selenium" on cmd.exe).
        1-5.Python Package-Numpy (After install Python 3.7 , command "pip3 install numpy" on cmd).
 	1-6.Python Package-Configparser (After install Python 3.7 , command "pip3 install configparser" on cmd).
2.Enabling all "import" in "auto-elearning.py".
3.If you don't whant use Intranet, can set "ocr" True in "auto-elearning.py".
3.Command "python auto-elearning.py" on cmd.exe.
4.Down!